{
  "mcpServers": {
    "laya-guard": {
      "command": "bash",
      "args": [
        "-c",
        "PY=${LAYA_PYTHON:-$([ \"$OS\" = Windows_NT ] && echo python || echo python3)}; exec \"$PY\" \"$0\"",
        "${CLAUDE_PLUGIN_ROOT}/laya_mcp_server.py"
      ]
    }
  }
}
